apiVersion: v1
kind: ServiceAccount
metadata: { name: product, namespace: staging }
---
apiVersion: apps/v1
kind: Deployment
metadata: { name: product, namespace: staging }
spec:
    replicas: 1
    selector: { matchLabels: { app: product } }
    template:
        metadata: { labels: { app: product } }
        spec:
            serviceAccountName: product
            containers:
                - name: product
                  image: lebaoo12/yas-product:main
                  ports: [{ containerPort: 8080 }]
                  env:
                      - {
                            name: SPRING_DATASOURCE_URL,
                            value: "jdbc:postgresql://postgres.default.svc.cluster.local:5432/staging_product",
                        }
                      - { name: SPRING_DATASOURCE_USERNAME, value: "admin" }
                      - { name: SPRING_DATASOURCE_PASSWORD, value: "admin" }
                      - { name: SERVER_PORT, value: "8080" }
                      - { name: SERVER_SERVLET_CONTEXT_PATH, value: "/product" }
                      - {
                            name: YAS_SERVICES_MEDIA,
                            value: "http://media/media",
                        }
                      - { name: JAVA_TOOL_OPTIONS, value: "-Xmx512m" }
                  resources: { requests: { memory: "256Mi", cpu: "50m" } }
---
apiVersion: v1
kind: Service
metadata: { name: product, namespace: staging }
spec:
    selector: { app: product }
    ports: [{ name: http, port: 80, targetPort: 8080 }]
